import React,{ Componment } from "react-native";
import {

} from "react-native";

export default class extends React.Componment {
    constructor(props) {
        super(props);
        this.state = {
            
        } 
    }
}